{
  "name": "hodynnyk-calendar",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check public/app.js && node --check public/admin.js && node --check public/pwa.js && node --check public/sw.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
